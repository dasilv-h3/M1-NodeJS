import 'package:flutter/material.dart';

class SectionFeminineJuniorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Section Féminine Junior')),
      body: Center(child: Text('Détails de la section féminine junior')),
    );
  }
}
