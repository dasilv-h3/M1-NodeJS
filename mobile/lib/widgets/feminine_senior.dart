import 'package:flutter/material.dart';

class SectionFeminineSeniorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Section Féminine Senior')),
      body: Center(child: Text('Détails de la section féminine senior')),
    );
  }
}
