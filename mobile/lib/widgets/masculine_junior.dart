import 'package:flutter/material.dart';

class SectionMasculineJuniorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Section Masculine Junior')),
      body: Center(child: Text('Détails de la section masculine junior')),
    );
  }
}
