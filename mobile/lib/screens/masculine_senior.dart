import 'package:flutter/material.dart';

class SectionMasculineSeniorScreen extends StatelessWidget {
  const SectionMasculineSeniorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Section Masculine Senior')),
      body: Center(child: Text('Détails de la section masculine senior')),
    );
  }
}
